function [teta, phi] = fcn(P,v)

x = P(1);
y = P(2);
z = P(3);
x_v = v(1);

y_v = v(2);

z_v = v(3);

 %-------------------------------------------------------------------------
 L1 = 0 ;

 L2 = 2.3;

 L3 = 4.5;
 
 % teta = q1;
 % 
 % phi = q2 + pi/2;
 % 
 % x = L3*cos(phi)*cos(teta);
 % 
 % y = L3*cos(phi)*sin(teta);
 % 
 % z = L3*cos(phi);   

%-------------------------------------------------------------------------

CV = [x_v-x; y_v-y; z_v-z];

%-------------------------------------------------------------------------

 teta = acos(CV(1)/norm(CV));

 phi = asin(CV(3)/norm(CV));

end
